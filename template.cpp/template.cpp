/**
 * 	author:  GMKanat
 * 	created: 16.01.2021 14:56:33
**/
#include <bits/stdc++.h>
using namespace std;

#define gg return 0
#define wp break
#define gl continue
#define next 
#define ll long long
#define NeedForSpeed ios::sync_with_stdio(0); cin.tie(0); cout.tie(0)
#define input freopen("input.txt","rt", stdin)
#define output freopen("output.txt","wt", stdout)

#define all(x) x.begin(), x.end()
#define rall(x) (x).rbegin(), (x).rend() 
#define sort(x) sort(all(x)) 
#define sz(x) (int)(x).size()
#define forn(i,n) for(int i = 0; i < (int)n; i++)
#define rforn(i, n) for(ll i = n - 1; i >= 0; i--)
#define forl(i,n) for(int i = 1;i <= n;i++)
#define fork(i,a,b) for (int i = a; i < b; i++)
#define gcd(a,b) __gcd(a,b)
#define lcm(a, b) ((a)*(b)/gcd(a,b))
#define unikue(x) (x).erase(unique(all(x)),(x).end())
#define pbb(x) x.pop_back()
#define fix(x) cout<<fixed<<setprecision(x)
#define MAX(a) *max_element(a.begin(), a.end())
#define MIN(a) *min_element(a.begin(), a.end())
#define SUM(a) accumulate(a.begin(), a.end(), 0)

#define tcase int t; cin>>t; while(t--)
#define fi first	
#define se second
#define mp make_pair
#define pb push_back
#define eb emplace_back 
#define imn INT_MIN
#define imx INT_MAX

int main(){
	NeedForSpeed;
	//freopen("input.txt", "r+", stdin);
}